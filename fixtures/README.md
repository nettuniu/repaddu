# Fixtures

## `picobench` submodule

This repository includes `fixtures/picobench` as a git submodule pinned to a specific upstream commit for stable analysis tests.

- Upstream: <https://github.com/iboB/picobench>
- Pinned commit: `c564754b934a24cd58d9fbaec805f1c6d83b9b27`

Initialize/update after clone:

```bash
git submodule update --init --recursive fixtures/picobench
```
